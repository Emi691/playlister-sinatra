<!DOCTYPE html>
<html>
  <head>
    <title><%=@artist.name%></title>
  </head>
  <body>
  <%@songs.each do |song|%>
  <p><a href="/songs/<%=song.slug%>"><%=song.name%></p>
  <%end%>
  <%@genres.each do |genre|%>
  <p><a href="/genres/<%=genre.slug%>"><%=genre.name%></p>
  <%end%>
  </body>
</html>